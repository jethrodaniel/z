#include <catch.hpp>

#include <holycc/holycc.hpp>

SCENARIO("holycc") {
  GIVEN("todo: something") {
    WHEN("todo: something happens") {
      THEN("todo: we get this") {
        REQUIRE(foo() == 0);
      }
    }
  }
}
